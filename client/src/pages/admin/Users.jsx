import {useEffect,useState} from "react";import api from "../../services/api";
export default function Users(){const [users,setUsers]=useState([]);async function load(){const {data}=await api.get("/admin/users");setUsers(data)}useEffect(()=>{load()},[]);async function block(id){await api.put(`/admin/users/${id}/block`);load()}return <main className="container py-10"><h1 className="text-4xl font-black">Manage Users</h1><div className="mt-8 overflow-auto card"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Name</th><th>Email</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr className="border-b last:border-0" key={u._id}><td className="p-3">{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.blocked?"Blocked":"Active"}</td><td><button className="text-indigo-600" onClick={()=>block(u._id)}>{u.blocked?"Unblock":"Block"}</button></td></tr>)}</tbody></table></div></main>}
